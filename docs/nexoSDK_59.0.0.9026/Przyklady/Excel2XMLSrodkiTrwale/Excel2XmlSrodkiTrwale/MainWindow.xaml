<Window x:Class="Excel2XmlSrodkiTrwale.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Excel2XmlSrodkiTrwale"
        mc:Ignorable="d"
         Title="Eksportowanie danych z Excela do XML dla przeniesienia danych do nexo" Height="138" Width="575" MinHeight="250" MinWidth="575" >
    <Grid ShowGridLines="False" Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="4" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="Margin" Value="4" />
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Margin" Value="4" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="4" />
                <Setter Property="Padding" Value="6,4" />
            </Style>
        </Grid.Resources>

        <Label Content="_Plik Excela (XLSX):" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PlikExcela}"></TextBox>
        <Button Grid.Row="0" Grid.Column="2" Click="Wybierz_Click">Wybierz</Button>

        <StackPanel Orientation="Horizontal" Grid.Row="10" Grid.ColumnSpan="10" HorizontalAlignment="Right" >
            <Button IsDefault="True" x:Name="ButtonSzerszy" Click="OK_Click" Margin="4,-42,4,4" Width="136">Generuj XML-e</Button>
        </StackPanel>
        <Label Content="_Rodzaj danych:" Grid.Row="1" />
        <ComboBox Grid.Row="1" Grid.Column="1" SelectedItem="{Binding RodzajDanych}" ItemsSource="{Binding RodzajeDanych}" Width="217" HorizontalAlignment="Left" Margin="4,4,0,4"/>
    </Grid>
</Window>
